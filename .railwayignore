# Never upload secrets — Railway holds these in its own Variables tab.
.env

# Installed fresh on the build machine from package-lock.json.
# (Uploading Windows-built modules to a Linux builder is both slow and unsafe.)
node_modules/

# Local editor / agent config — not needed at runtime.
.claude/
